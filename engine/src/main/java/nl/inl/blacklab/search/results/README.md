Classes for dealing with search results: `Hits`, `DocResults`, `HitGroups`,
`DocGroups`. All derive from `Result`.

Sorting and grouping can be done with `HitProperty`, `DocProperty`, 
`HitGroupProperty`, etc.

