package nl.inl.blacklab.index;

/**
 * Ignores all progress reports from the indexer.
 */
public class IndexListenerDevNull extends IndexListener {
    // Convenience class, doesn't actually add anything
}
