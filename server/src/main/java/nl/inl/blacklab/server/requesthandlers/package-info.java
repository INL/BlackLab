/**
 * Classes handling BLS's different requests, and other classes
 * that are closely tied to the current way BLS handles HTTP requests and produces responses.
 *
 * Anything in this package is probably not reusable for alternative implementations of
 * a BlackLab webservice.
 */
package nl.inl.blacklab.server.requesthandlers;
